<img src="{{ section.settings.image_with_text_image | img_url: 'medium' }}" alt="">


{%  schema  %}  

    {
        "name": "image section",
        "settings": [
            {
                "type": "image_picker",
                "id": "image_with_text_image",
                "label": "Image"
            }
        ],
        "presets": [
            {
                "name": "image section"
            }
        ]
    }

{% endschema %}